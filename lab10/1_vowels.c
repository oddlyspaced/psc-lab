#include<stdio.h>
int isVowel(char);
int isCons(char);

void main() {
    printf("Enter string: \n");
    char str[100];
    gets(str);

    for (int i = 0; i < strlen(str); i++) {
        
    }
    printf("%s", str);

}